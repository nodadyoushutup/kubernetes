apiVersion: batch/v1
kind: CronJob
metadata:
  name: radarr-cronjob-tarshot
  namespace: radarr
spec:
  schedule: "0 0 * * *"  # This schedule runs the job once a day at midnight
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: radarr-sa
          containers:
          - name: tarshot
            image: bitnami/kubectl:latest
            command: ["/bin/sh", "/scripts/tarshot_run.sh"]
            volumeMounts:
            - name: script
              mountPath: /scripts
          restartPolicy: OnFailure
          volumes:
          - name: script
            configMap:
              name: radarr-cm
              defaultMode: 0755
