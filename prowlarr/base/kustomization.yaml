# prowlarr/base/kustomization.yaml
resources:
  - persistentvolumeclaim.yaml
  - role.yaml
  - rolebinding.yaml

commonLabels:
  app: prowlarr

commonAnnotations:
  app: prowlarr-annontations

# namePrefix:
#   prowlarr-
